spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
  servlet:
    multipart:
      max-file-size: 250MB
      max-request-size: 250MB
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: digital_distribution

  thymeleaf:
    cache: false

server:
  port: 8080

openai:
  api-key: ${OPENAI_API_KEY}
  base-url: https://api.openai.com/v1

posters:
  upload-root: uploads
